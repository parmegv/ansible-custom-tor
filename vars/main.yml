---
# vars file for ansible-custom-tor
apt_dependencies:
  - libssl-dev
  - openssl
  - zlib1g-dev
  - libevent-dev
  - autoconf
  - automake
  - make

systemd_unit: "{{ src_dir }}/contrib/dist/tor.service"
systemd_units_path: "/etc/systemd/system/"
